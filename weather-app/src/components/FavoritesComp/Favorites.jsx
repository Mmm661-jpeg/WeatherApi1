


function Favorites({myFav,setMyFavs})
{
    const AddFav = () =>
    {
        
    }

    const RemoveFav = () =>
    {
       
    }

    return(

        <header> 

        <nav>
            <ul>

            </ul>  
        </nav>

        <button onClick={()=>AddFav}>Add to Favorites</button>
        <button onClick={()=>RemoveFav}>Add to Favorites</button>

    </header>

    )
}

export default Favorites